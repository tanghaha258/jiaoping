# 11 测试与验收方案

## 测试目标

- 验证 MVP 业务闭环可用。
- 验证前后端接口契约一致。
- 验证角色权限和数据隔离有效。
- 验证桂教通接入、人工回填和 Mock 兜底可运行。
- 验证 2核2G4M 服务器部署可承载演示。

## 测试环境

至少准备：

- 本地开发环境。
- 演示部署环境。
- 测试账号：系统管理员、学校管理员、教师、学生、教研员。

## 功能测试

### 登录认证

| 用例 | 预期 |
| --- | --- |
| 正确账号密码登录 | 返回 token 和用户信息 |
| 错误密码登录 | 返回错误，不泄露具体原因 |
| 连续失败 5 次 | 账号临时锁定 |
| token 过期刷新 | 返回新 token |
| 退出登录 | token 不再可用 |

### 项目流程

| 用例 | 预期 |
| --- | --- |
| 教师创建项目 | 项目状态为 draft |
| 教师启动项目 | 状态变为 active |
| 教师结项 | 状态变为 completed |
| 归档项目 | 项目只读 |
| 学生访问未发布项目 | 拒绝访问 |

### 任务与提交

| 用例 | 预期 |
| --- | --- |
| 教师创建任务 | 任务为 draft |
| 教师发布任务 | 学生可见 |
| 学生提交文本 | 提交成功 |
| 学生上传合法附件 | 上传成功 |
| 学生上传非法类型 | 拒绝 |
| 截止后提交 | 按规则拒绝或标记逾期 |

### 评价

| 用例 | 预期 |
| --- | --- |
| 教师创建量规 | 保存维度和等级 |
| 教师评价提交 | 生成评价记录 |
| 学生查看反馈 | 只能查看自己的反馈 |
| 学生自评 | 生成 self 类型评价 |
| 教师确认评价 | 状态变为 confirmed |

### AI 调用

| 用例 | 预期 |
| --- | --- |
| Mock 生成教学设计 | 生成可审核结果 |
| 桂教通链接模式 | 生成待回填记录 |
| 人工回填输出 | 状态变为 imported |
| 教师审核通过 | 状态变为 reviewed |
| 教师采纳 | 内容写入业务对象 |
| Provider 超时 | 返回明确错误并留痕 |

## 权限测试

必须验证：

- 学生不能访问管理端。
- 学生不能查看其他学生提交。
- 教师不能查看无关班级提交。
- 学校管理员不能查看其他学校数据。
- 区域管理员默认看到聚合数据。
- 未登录不能访问业务接口。

## 安全测试

验证：

- 密码不明文存储。
- API 密钥不返回前端。
- 上传脚本文件被拒绝。
- 越权访问返回 403。
- 删除和导出操作写入审计日志。
- AI 输入脱敏规则生效。
- AI 风险输出不能采纳。

## 接口测试

每个核心接口验证：

- 请求参数校验。
- 正常响应格式。
- 错误响应格式。
- `trace_id` 存在。
- 分页参数有效。
- 未授权和越权处理。

建议后端用 pytest，前端用接口 Mock 或测试环境联调。

## 部署验收

| 项目 | 标准 |
| --- | --- |
| 前端访问 | 首页和登录页正常 |
| 后端健康检查 | `/api/v1/health` 返回 ok |
| 数据库 | 迁移成功，可读写 |
| 上传 | 文件可上传并鉴权下载 |
| HTTPS | 正式域名启用证书 |
| 日志 | 后端和 Nginx 日志可查看 |
| 备份 | 数据库和 uploads 可备份 |

## 比赛演示验收

演示链路：

1. 教师登录。
2. 创建“保护海洋，从我做起”项目。
3. 调用桂教通智能体或进入桂教通回填模式。
4. 保存教学设计。
5. 发布学习任务。
6. 学生登录并提交作品。
7. 教师评价。
8. 生成教学反思。
9. 管理端查看数据驾驶舱和 AI 调用记录。

验收口径：

- 评委能看见完整教学闭环。
- 评委能看见桂教通使用证据。
- 评委能看见安全和权限边界。
- 系统运行流畅，不依赖复杂人工解释。

## 缺陷等级

| 等级 | 定义 |
| --- | --- |
| P0 | 系统无法登录、核心流程无法演示、数据严重泄露 |
| P1 | MVP 核心功能不可用或明显越权 |
| P2 | 单个模块异常但有绕行方案 |
| P3 | 文案、样式、小交互问题 |

P0/P1 必须在演示前修复。

