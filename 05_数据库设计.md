# 05 数据库设计

## 设计原则

- 使用 PostgreSQL 15+。
- 主键统一使用 UUID。
- 时间字段统一包含 `created_at`、`updated_at`。
- 重要业务表包含 `created_by`、`updated_by`。
- 删除优先软删除，字段为 `deleted_at`。
- 组织隔离字段必须明确，例如 `school_id`、`region_id`。
- AI 原始请求和响应可存 JSONB 或文件路径，避免在主表中堆积大文本。

## 核心实体关系

```text
regions -> schools -> classes -> students
schools -> users
users -> roles
projects -> tasks -> submissions -> evaluations
projects -> ai_agent_calls
resources -> resource_tags
rubrics -> rubric_items
```

## 表设计

### users

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| username | varchar(64) | 登录名，唯一 |
| password_hash | varchar(255) | 密码哈希 |
| name | varchar(64) | 姓名 |
| role | varchar(32) | 角色 |
| school_id | uuid | 所属学校，可空 |
| class_id | uuid | 学生所属班级，可空 |
| status | varchar(16) | active/disabled/locked |
| last_login_at | timestamptz | 最近登录 |
| created_at | timestamptz | 创建时间 |
| updated_at | timestamptz | 更新时间 |

索引：

- `idx_users_username`
- `idx_users_school_role`

### roles

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| code | varchar(32) | 角色代码 |
| name | varchar(64) | 角色名称 |
| permissions | jsonb | 权限点数组 |

MVP 可先使用固定角色枚举，后续再做完整角色表管理。

### regions

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| name | varchar(128) | 区域名称 |
| code | varchar(64) | 区域代码 |

### schools

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| region_id | uuid | 所属区域 |
| name | varchar(128) | 学校名称 |
| code | varchar(64) | 学校代码 |
| status | varchar(16) | 状态 |

### classes

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| school_id | uuid | 所属学校 |
| grade | varchar(32) | 年级 |
| name | varchar(64) | 班级名称 |
| academic_year | varchar(16) | 学年 |

### subjects

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| name | varchar(64) | 学科名称 |
| stage | varchar(32) | 学段 |

### projects

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| school_id | uuid | 所属学校 |
| name | varchar(128) | 项目名称 |
| grade | varchar(32) | 年级 |
| driving_question | text | 驱动问题 |
| objectives | jsonb | 教学目标 |
| lesson_count | int | 课时数 |
| status | varchar(16) | draft/active/completed/archived |
| owner_id | uuid | 创建教师 |
| created_at | timestamptz | 创建时间 |

关联表：

- `project_subjects(project_id, subject_id)`
- `project_classes(project_id, class_id)`

### project_lessons

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| project_id | uuid | 项目 |
| title | varchar(128) | 课时标题 |
| lesson_order | int | 顺序 |
| content | jsonb | 课时内容 |

### tasks

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| project_id | uuid | 项目 |
| title | varchar(128) | 任务标题 |
| description | text | 任务说明 |
| task_type | varchar(32) | individual/group/classroom/homework |
| submit_type | varchar(32) | text/file/link/mixed |
| rubric_id | uuid | 评价量规 |
| due_at | timestamptz | 截止时间 |
| status | varchar(16) | draft/published/closed/archived |

### submissions

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| task_id | uuid | 任务 |
| student_id | uuid | 学生用户 |
| group_name | varchar(64) | 小组名，可空 |
| content | text | 文本内容 |
| attachments | jsonb | 附件列表 |
| status | varchar(16) | draft/submitted/reviewed/returned/revised |
| submitted_at | timestamptz | 提交时间 |

### rubrics

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| school_id | uuid | 所属学校，可空 |
| name | varchar(128) | 量规名称 |
| description | text | 说明 |
| scope | varchar(16) | personal/school/region/system |
| created_by | uuid | 创建人 |

### rubric_items

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| rubric_id | uuid | 量规 |
| dimension | varchar(64) | 维度 |
| weight | numeric(5,2) | 权重 |
| levels | jsonb | 等级描述 |
| sort_order | int | 排序 |

### evaluations

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| submission_id | uuid | 提交 |
| evaluator_id | uuid | 评价人 |
| evaluator_type | varchar(16) | teacher/self/peer/ai |
| rubric_id | uuid | 量规 |
| scores | jsonb | 维度分数 |
| comments | text | 评语 |
| status | varchar(16) | draft/confirmed |
| confirmed_by | uuid | 教师确认人 |

### resources

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| school_id | uuid | 所属学校，可空 |
| title | varchar(128) | 标题 |
| resource_type | varchar(32) | 类型 |
| file_path | varchar(255) | 文件路径，可空 |
| url | varchar(512) | 外链，可空 |
| metadata | jsonb | 元数据 |
| visibility | varchar(16) | private/school/region/public |
| status | varchar(16) | draft/published/archived |

### ai_agents

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| name | varchar(128) | 智能体名称 |
| provider | varchar(32) | gjt_api/gjt_link/manual_import/mock |
| scenario | varchar(64) | 场景 |
| config | jsonb | 配置 |
| input_schema | jsonb | 输入字段定义 |
| output_schema | jsonb | 输出字段定义 |
| enabled | boolean | 是否启用 |

### ai_agent_calls

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| agent_id | uuid | 智能体 |
| user_id | uuid | 调用人 |
| school_id | uuid | 所属学校 |
| project_id | uuid | 关联项目，可空 |
| scenario | varchar(64) | 场景 |
| provider | varchar(32) | Provider |
| input_summary | text | 输入摘要 |
| output_summary | text | 输出摘要 |
| request_payload | jsonb | 脱敏后请求 |
| response_payload | jsonb | 响应 |
| status | varchar(16) | created/running/succeeded/failed/imported/reviewed/adopted |
| review_status | varchar(16) | pending/approved/rejected |
| error_message | text | 错误信息 |
| created_at | timestamptz | 创建时间 |

### audit_logs

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id | uuid | 主键 |
| user_id | uuid | 操作人 |
| action | varchar(64) | 操作 |
| target_type | varchar(64) | 对象类型 |
| target_id | uuid | 对象 ID |
| ip | inet | IP |
| user_agent | text | UA |
| detail | jsonb | 详情 |
| created_at | timestamptz | 时间 |

### system_settings

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| key | varchar(128) | 配置键 |
| value | jsonb | 配置值 |
| description | text | 说明 |

## 数据隔离规则

- 教师查询项目时必须限制 `owner_id` 或关联班级。
- 学生查询任务时必须限制其所在班级。
- 学校管理员查询时必须限制 `school_id`。
- 区域管理员查询时默认返回聚合数据，个体数据需额外权限。
- AI 调用记录必须带 `school_id` 和 `user_id`。

## 初始化数据

MVP 初始化：

- 一个系统管理员。
- 一个区域。
- 一个学校。
- 七年级一班。
- 若干教师和学生测试账号。
- 初始学科：语文、数学、英语、地理、生物、道法、信息科技。
- 初始 AI 智能体：跨学科教学设计、学情诊断、评价量规、教学反思。

