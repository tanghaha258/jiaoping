# 07 安全认证与合规边界

## 安全目标

- 确保不同角色只能访问授权数据。
- 确保学生个人信息最小化使用。
- 确保 AI 输出可审核、可追溯、可撤回。
- 确保关键操作留痕，便于学校和比赛答辩说明。

## 认证

采用：

```text
JWT Access Token + Refresh Token
```

策略：

- Access Token 默认 30 分钟过期。
- Refresh Token 默认 7 天过期。
- 密码使用 bcrypt 或 argon2 哈希。
- 登录失败 5 次锁定 15 分钟。
- 修改密码后旧 token 失效。
- 退出登录记录 token 撤销。

## 角色权限

角色：

- `system_admin`
- `region_admin`
- `school_admin`
- `researcher`
- `teacher`
- `student`
- `parent`

MVP 权限矩阵：

| 功能 | 系统 | 区域 | 学校 | 教研 | 教师 | 学生 |
| --- | --- | --- | --- | --- | --- | --- |
| 系统配置 | 是 | 否 | 否 | 否 | 否 | 否 |
| 学校管理 | 是 | 是 | 否 | 否 | 否 | 否 |
| 本校用户 | 是 | 否 | 是 | 否 | 否 | 否 |
| 主题模板 | 是 | 是 | 是 | 是 | 读 | 读 |
| 项目创建 | 是 | 否 | 否 | 是 | 是 | 否 |
| 任务提交 | 否 | 否 | 否 | 否 | 否 | 是 |
| 教师评价 | 否 | 否 | 否 | 否 | 是 | 否 |
| AI 配置 | 是 | 否 | 可选 | 否 | 否 | 否 |
| AI 调用 | 是 | 是 | 是 | 是 | 是 | 受限 |
| 审计日志 | 是 | 聚合 | 本校 | 否 | 本人 | 否 |

## 数据范围控制

每个接口必须校验：

- 当前用户角色。
- 当前用户组织范围。
- 请求资源所属组织。
- 是否与班级、项目或任务存在授权关系。

示例：

- 教师只能访问自己创建或被授权班级的项目。
- 学生只能访问自己班级已发布任务。
- 学校管理员只能访问本校用户和项目。
- 区域管理员默认不查看学生提交原文。

## AI 安全边界

AI 可以：

- 生成教学设计草案。
- 生成活动建议。
- 生成评价量规草案。
- 对班级整体学情给出教学建议。
- 生成教学反思草案。

AI 不可以：

- 自动给学生最终成绩。
- 对学生做心理、医疗、品行定性。
- 输出羞辱性排名或惩戒建议。
- 绕过教师直接向学生发布评价。
- 处理未经脱敏的敏感个人信息。

## 学生隐私

禁止在 AI 输入中包含：

- 身份证号。
- 手机号。
- 家庭住址。
- 家庭经济困难细节。
- 医疗或心理信息。
- 未授权照片。

对外展示数据必须聚合化：

- 可展示班级整体达成度。
- 可展示项目数量、资源数量、AI 调用次数。
- 不展示单个学生姓名和敏感提交内容。

## 文件安全

- 允许类型：pdf、docx、pptx、xlsx、jpg、png、txt、md。
- MVP 默认单文件限制 20MB。
- 文件保存使用随机文件名。
- 文件下载必须通过鉴权接口。
- 上传目录不得执行脚本。
- 删除资源要记录审计日志。

## 审计日志

必须记录：

- 登录成功和失败。
- 创建、修改、删除用户。
- 创建、启动、结项、归档项目。
- 发布任务。
- 修改评价。
- AI 调用、回填、审核、采纳。
- 文件上传和删除。
- 导出数据。
- 权限变更。

审计日志字段：

- 操作人。
- 操作类型。
- 对象类型。
- 对象 ID。
- IP。
- User-Agent。
- 操作详情。
- 时间。

## 安全响应

发现异常时：

- 多次登录失败：锁定账号。
- 越权访问：返回 403 并记录审计。
- AI 输出风险：标记为 `rejected`，禁止采纳。
- 文件类型非法：拒绝上传。
- Provider 调用异常：记录错误但不泄露密钥或完整堆栈。

## 合规表达

比赛答辩中建议使用：

```text
平台坚持“AI 辅助、教师主导、过程留痕、数据脱敏”的原则。
AI 只生成建议，不直接形成学生最终评价。
所有桂教通智能体使用均通过后台留痕，便于教学复盘和管理审计。
```

