# 10 部署运维手册

## 部署目标

目标服务器：

```text
2 核 CPU
2GB 内存
4Mbps 带宽
Linux 服务器
```

部署方式：

```text
Nginx + FastAPI + PostgreSQL + 静态前端
Docker Compose 管理服务
```

## 推荐目录

```text
/opt/ai-edu/
├─ docker-compose.yml
├─ .env
├─ nginx/
│  └─ default.conf
├─ backend/
├─ frontend-dist/
├─ uploads/
├─ backups/
└─ logs/
```

## Docker Compose 服务

服务：

- `nginx`
- `backend`
- `postgres`

不在 MVP 中部署：

- Redis。
- MinIO。
- Elasticsearch。
- Kubernetes。
- 本地大模型。

## 环境变量

`.env` 必填：

```text
APP_ENV=production
DOMAIN=example.com
DATABASE_URL=postgresql+asyncpg://ai_edu:password@postgres:5432/ai_edu
POSTGRES_DB=ai_edu
POSTGRES_USER=ai_edu
POSTGRES_PASSWORD=change_me
JWT_SECRET=change_me_to_long_random_string
UPLOAD_DIR=/app/uploads
MAX_UPLOAD_MB=20
GJT_API_BASE_URL=
GJT_API_KEY=
```

生产环境必须修改：

- `POSTGRES_PASSWORD`
- `JWT_SECRET`
- 桂教通相关密钥

## Nginx 路由

```text
/              -> frontend static files
/api/v1        -> backend:8000
/health        -> backend health check
```

上传文件建议通过后端鉴权下载，不直接开放 `/uploads`。

## 上线步骤

1. 安装 Docker 和 Docker Compose。
2. 上传代码和前端构建产物。
3. 创建 `.env`。
4. 启动 PostgreSQL。
5. 执行数据库迁移。
6. 初始化管理员账号。
7. 启动后端。
8. 启动 Nginx。
9. 配置 HTTPS。
10. 访问 `/api/v1/health` 和前端页面验证。

## HTTPS

推荐使用：

- 宝塔面板自动申请证书，或
- Certbot + Nginx。

所有正式演示环境必须启用 HTTPS，避免账号和 token 明文传输。

## 备份

每日备份：

- PostgreSQL 数据库。
- uploads 文件目录。
- `.env` 配置备份，单独加密保存。

建议备份目录：

```text
/opt/ai-edu/backups/YYYY-MM-DD/
```

保留策略：

- 最近 7 天每日备份。
- 最近 4 周每周备份。
- 重要答辩前手动备份。

## 恢复

恢复顺序：

1. 停止后端服务。
2. 恢复 PostgreSQL。
3. 恢复 uploads。
4. 检查 `.env`。
5. 启动服务。
6. 登录管理员账号验证。

## 健康检查

后端提供：

```text
GET /api/v1/health
```

返回：

```json
{
  "status": "ok",
  "database": "ok",
  "version": "0.1.0"
}
```

## 资源控制

- 后端 Uvicorn 默认 1 worker。
- 数据库连接池控制在 5 左右。
- 上传文件限制 20MB。
- 日志按日期轮转。
- 驾驶舱不要设置过高刷新频率。
- AI 调用设置超时和并发限制。

## 运维检查清单

上线前：

- `.env` 密钥已修改。
- 数据库迁移成功。
- 管理员账号可登录。
- HTTPS 可访问。
- 上传目录可写。
- 审计日志正常记录。
- AI Mock 可用；桂教通智能体对接可在后续版本启用。

答辩前：

- 准备演示账号。
- 准备一个完整主题项目。
- 准备 Mock AI 调用记录。
- 准备后续桂教通对接的账号、接口地址和密钥清单。
