# 08 前端开发规范

## 技术栈

- Vue 3。
- Vite。
- TypeScript。
- Element Plus。
- Pinia。
- Vue Router。
- Axios。
- ECharts，可用于管理端图表。

## 项目结构

```text
frontend/
├─ src/
│  ├─ api/
│  ├─ assets/
│  ├─ components/
│  ├─ layouts/
│  ├─ router/
│  ├─ stores/
│  ├─ types/
│  ├─ utils/
│  └─ views/
├─ public/
├─ index.html
├─ package.json
└─ vite.config.ts
```

## 路由规划

```text
/login
/
/teacher
/teacher/projects
/teacher/projects/:id
/teacher/projects/:id/ai
/teacher/tasks/:id/submissions
/teacher/evaluations

/student
/student/tasks
/student/tasks/:id
/student/submissions/:id
/student/profile

/research
/research/templates
/research/resources

/admin
/admin/dashboard
/admin/users
/admin/schools
/admin/ai-agents
/admin/ai-calls
/admin/audit-logs
/admin/settings
```

## 权限守卫

路由元信息：

```ts
{
  path: '/admin/users',
  component: UserList,
  meta: {
    requiresAuth: true,
    roles: ['system_admin', 'school_admin']
  }
}
```

守卫规则：

- 未登录访问业务页跳转 `/login`。
- 已登录访问 `/login` 跳转对应工作台。
- 角色不匹配显示 403 页面。
- Access Token 过期时尝试刷新；刷新失败跳转登录。

## API 封装

统一 Axios 实例：

- 自动添加 Authorization。
- 自动处理 `trace_id`。
- 统一错误提示。
- 401 自动刷新 token。
- 403 跳转无权限页。

接口文件按模块拆分：

```text
api/auth.ts
api/projects.ts
api/tasks.ts
api/evaluations.ts
api/resources.ts
api/ai.ts
api/dashboard.ts
```

## 状态管理

Pinia Store：

- `useAuthStore`：token、当前用户、权限。
- `useAppStore`：菜单、布局、全局 loading。
- `useProjectStore`：当前项目上下文。

业务列表数据不长期放 store，优先由页面请求。

## 页面设计原则

- 教师端强调流程效率，使用工作台、表单、表格、步骤条。
- 学生端强调任务清晰，减少管理性入口。
- 管理端强调数据扫描，使用表格、筛选、图表。
- 不做营销落地页，第一屏就是实际工作台或登录页。
- 所有列表必须分页。
- 所有删除操作必须二次确认。

## 核心组件

建议组件：

- `AppLayout`：主布局。
- `RoleMenu`：角色菜单。
- `DataTable`：表格封装。
- `SearchBar`：查询区。
- `StatusTag`：状态标签。
- `AiCallPanel`：AI 调用面板。
- `RubricEditor`：量规编辑器。
- `FileUploader`：文件上传。
- `AuditLogDrawer`：审计详情。

## AI 调用交互

AI 面板状态：

```text
未调用 -> 调用中 -> 生成成功 -> 待审核 -> 已采纳
                 -> 失败
                 -> 待回填
```

界面要求：

- 清楚显示 Provider：桂教通 API、桂教通链接、人工回填、Mock。
- 如果是跳转模式，显示“前往桂教通生成”。
- 如果是人工回填，提供粘贴框和凭证上传。
- 如果是 Mock，必须显示演示标识。
- AI 输出默认可编辑，教师确认后才保存。

## UI 规范

- 主色建议使用教育科技蓝，但避免整站只有单一蓝色。
- 后台卡片圆角不超过 8px。
- 操作按钮尽量使用图标 + 文本。
- 表格操作列固定宽度。
- 移动端至少保证学生任务页可用。
- 空状态提供明确下一步操作。

## 前端验收

- 登录、刷新、退出流程正常。
- 不同角色菜单不同。
- 无权限路由不可访问。
- 教师可完整创建项目、调用 AI、发布任务。
- 学生可查看任务并提交。
- 管理端图表和列表可加载。
- 小屏宽度下学生端主要流程无明显错位。

