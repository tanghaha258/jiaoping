# 09 后端开发规范

## 技术栈

- Python 3.11+。
- FastAPI。
- SQLAlchemy 2.x。
- Alembic。
- Pydantic v2。
- PostgreSQL。
- Uvicorn。
- PyJWT 或 python-jose。
- passlib/bcrypt 或 argon2-cffi。

## 项目结构

```text
backend/
├─ app/
│  ├─ api/
│  │  └─ v1/
│  ├─ core/
│  ├─ db/
│  ├─ models/
│  ├─ schemas/
│  ├─ services/
│  ├─ repositories/
│  ├─ providers/
│  └─ main.py
├─ alembic/
├─ tests/
├─ pyproject.toml
└─ Dockerfile
```

## 分层规则

- Router 只处理 HTTP、依赖注入和响应包装。
- Service 处理业务规则、权限和事务。
- Repository 处理数据库查询。
- Model 只定义 ORM 映射。
- Schema 只定义输入输出模型。
- Provider 只封装外部服务。

禁止在 Router 中写复杂 SQL 或 AI 调用细节。

## 配置管理

环境变量：

```text
APP_ENV=production
DATABASE_URL=postgresql+psycopg://...
JWT_SECRET=...
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
UPLOAD_DIR=/app/uploads
MAX_UPLOAD_MB=20
GJT_API_BASE_URL=
GJT_API_KEY=
```

敏感配置不得提交到代码仓库。

## 异常处理

统一业务异常：

```python
class AppException(Exception):
    def __init__(self, code: int, message: str, status_code: int = 400):
        ...
```

全局异常处理器负责输出统一响应格式。

## 日志规范

日志必须包含：

- `trace_id`
- `user_id`
- `path`
- `method`
- `status_code`
- `duration_ms`

禁止在日志中输出：

- 明文密码。
- JWT。
- API 密钥。
- 学生敏感信息。

## 权限依赖

推荐依赖：

```python
get_current_user()
require_roles(*roles)
require_permission(permission)
require_school_scope(resource_school_id)
```

业务查询必须在 Service 层再次校验数据范围，不能只依赖前端隐藏按钮。

## 数据库事务

- 单个写接口使用一个事务。
- AI 调用记录创建和状态更新要独立可靠。
- 外部 AI 调用不应长时间占用数据库事务。
- 先创建调用记录，再调用 Provider，最后更新状态。

## 文件上传

流程：

```text
校验登录
-> 校验文件大小和类型
-> 生成随机文件名
-> 保存到 uploads
-> 写入 resources 或附件记录
-> 返回文件 ID
```

文件下载优先通过后端鉴权后返回，不直接暴露真实路径。

## AI Provider

接口：

```python
class BaseAIProvider:
    async def run(self, request: AIProviderRequest) -> AIProviderResult:
        ...
```

Provider 实现：

- `GjtApiProvider`
- `GjtLinkProvider`
- `ManualImportProvider`
- `MockProvider`

AI Gateway 负责选择 Provider，不允许业务 Service 直接调用具体 Provider。

## 审计日志

写操作必须调用审计服务：

```python
audit_log(
    user=current_user,
    action="project.create",
    target_type="project",
    target_id=project.id,
    detail={...}
)
```

审计失败不应阻断主业务，但必须记录服务端错误日志。

## 后端测试

至少包含：

- 登录成功/失败。
- 角色权限。
- 项目创建。
- 任务发布。
- 学生提交。
- 教师评价。
- AI 调用 Mock。
- 人工回填。
- 越权访问被拒绝。

