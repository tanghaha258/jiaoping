# 06 AI 智能体接入规范

## 设计目标

- 满足比赛对桂教通 AI 平台使用的要求。
- 不把核心业务能力绑定到单一外部平台。
- 支持 API、链接/嵌入、人工回填和本地 Mock。
- 对 AI 输入输出进行脱敏、审核、留痕和可追溯管理。

## 架构

```text
业务场景
  -> AI Gateway
     -> 输入组装
     -> 脱敏处理
     -> 频率限制
     -> Provider Adapter
        -> gjt_api
        -> gjt_link
        -> manual_import
        -> mock
     -> 输出校验
     -> 安全审核
     -> 调用留痕
```

## Provider 类型

| Provider | 说明 | 适用情况 |
| --- | --- | --- |
| `gjt_api` | 后端调用桂教通开放接口 | 桂教通提供正式 API |
| `gjt_link` | 跳转或嵌入桂教通智能体页面 | 桂教通提供智能体链接 |
| `manual_import` | 教师复制桂教通输出回填 | 无 API、无嵌入能力时 |
| `mock` | 本地演示数据 | 开发、测试、断网演示 |

## 智能体场景

| 场景 | code | 输入 | 输出 |
| --- | --- | --- | --- |
| 学情诊断 | `learning_diagnosis` | 班级、前测、观察记录 | 班级画像、分层建议 |
| 跨学科教学设计 | `lesson_plan` | 年级、学科、主题、课时 | 教学设计、任务链 |
| 评价量规生成 | `rubric_generation` | 目标、任务、维度 | 量规项和等级描述 |
| 资源推荐 | `resource_recommendation` | 主题、学科、教材 | 资源建议 |
| 教学反思 | `teaching_reflection` | 评价结果、过程数据 | 反思与改进建议 |

## 调用流程

```text
1. 用户在业务页面选择 AI 助手。
2. 前端提交结构化输入到 `/api/v1/ai/calls`。
3. 后端校验用户权限和场景权限。
4. AI Gateway 按智能体配置选择 Provider。
5. 系统生成脱敏输入和输入摘要。
6. Provider 返回结果或生成跳转/回填任务。
7. 系统保存调用记录。
8. 教师审核、修改、确认。
9. 用户选择“采纳”后写入项目、任务、量规或反思。
```

## `gjt_api` 模式

配置示例：

```json
{
  "endpoint": "https://example-gjt-api/agents/{agent_id}/runs",
  "agent_external_id": "gjt_lesson_plan_agent",
  "auth_type": "bearer",
  "timeout_seconds": 30
}
```

实现要求：

- 密钥只存后端环境变量或加密配置，不返回前端。
- 失败时记录错误码和错误信息。
- 超时默认 30 秒。
- 结果需经过输出 schema 校验。

## `gjt_link` 模式

配置示例：

```json
{
  "url": "https://gjt.example.com/agent/share/xxx",
  "open_mode": "new_tab",
  "supports_iframe": false
}
```

前端行为：

- 用户点击“前往桂教通生成”。
- 平台生成一条 `ai_agent_calls` 记录，状态为 `created`。
- 教师在桂教通完成生成后复制结果回平台。
- 平台通过 `/ai/calls/{id}/import` 保存结果。

## `manual_import` 模式

必须填写：

- 桂教通智能体名称。
- 使用时间。
- 输入摘要。
- 输出正文。
- 是否采纳。

可选上传：

- 截图。
- 导出文件。
- 链接。

此模式是比赛答辩中的稳妥兜底，能证明使用了桂教通，同时不依赖接口开放。

## `mock` 模式

仅用于：

- 本地开发。
- 演示环境兜底。
- 自动化测试。

Mock 输出必须标记 `provider=mock`，不得在正式答辩中冒充桂教通调用。

## 输入脱敏规则

禁止发送：

- 学生身份证号、手机号、家庭住址。
- 未经授权的个人照片。
- 心理健康、医疗、家庭困难等敏感信息。
- 可直接识别单个学生的惩戒性描述。

允许发送：

- 年级、学科、教材版本。
- 班级整体表现摘要。
- 匿名化学生表现。
- 任务和评价维度。

示例：

```text
不推荐：张三上课经常不听讲，成绩倒数。
推荐：班级中约 15% 学生在资料提取和表达组织方面需要更多支架。
```

## 输出审核规则

AI 输出必须经过：

- 敏感词检查。
- 结构校验。
- 教师人工确认。
- 采纳记录保存。

AI 不得直接发布到学生端，除非教师确认。

## 提示词模板原则

模板结构：

```text
角色：你是初中跨学科主题学习教学设计助手。
任务：根据输入生成结构化教学建议。
约束：符合初中学生认知水平，不做最终评价结论。
输入：{structured_input}
输出格式：JSON 或 Markdown 分节。
安全要求：不得输出歧视、惩戒、心理诊断内容。
```

## 调用留痕

每次调用至少记录：

- 调用人。
- 学校。
- 智能体。
- Provider。
- 场景。
- 输入摘要。
- 输出摘要。
- 状态。
- 审核状态。
- 采纳状态。
- 时间。

## 失败降级

```text
gjt_api 失败
  -> 提示用户稍后重试
  -> 如果配置了 gjt_link，则提供跳转
  -> 如果无链接，则进入 manual_import
  -> 如果是演示环境，可使用 mock
```

不得静默切换 Provider。切换必须在界面上提示用户。

