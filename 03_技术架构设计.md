# 03 技术架构设计

## 架构目标

- 第一版可在 2核2G4M 轻量服务器稳定运行。
- 前后端分离，便于多智能体并行开发。
- 单体后端优先，避免微服务复杂度。
- 所有外部 AI 能力通过 AI Gateway 接入。
- 文件、数据库、日志、配置具备可备份和可迁移能力。

## 总体架构

```text
Browser
  |
  | HTTPS
  v
Nginx
  ├─ /              -> frontend static files
  ├─ /api/v1        -> FastAPI backend
  └─ /uploads       -> protected download via backend preferred

FastAPI Backend
  ├─ Auth & RBAC
  ├─ Project Service
  ├─ Task Service
  ├─ Evaluation Service
  ├─ Resource Service
  ├─ AI Gateway
  ├─ Dashboard Service
  └─ Audit Log

PostgreSQL
Uploads Directory
External AI Providers
  └─ 桂教通 AI Platform
```

## 部署组件

| 组件 | 运行方式 | 资源建议 |
| --- | --- | --- |
| Nginx | Docker 容器或宿主机 | 50MB |
| Frontend | 静态文件 | 无常驻进程 |
| Backend | Uvicorn 单进程 | 400-700MB |
| PostgreSQL | Docker 容器 | 400-700MB |
| Certbot | 可选 | 按需运行 |

服务器 2G 内存下不启动独立 Redis、MinIO、ES、本地大模型。

## 后端分层

```text
api routers
  -> schemas
  -> services
  -> repositories
  -> models
  -> database
```

职责：

- `routers`：HTTP 输入输出、鉴权依赖、参数校验。
- `schemas`：Pydantic 请求和响应模型。
- `services`：业务规则、权限检查、事务编排。
- `repositories`：数据库读写。
- `models`：SQLAlchemy ORM。
- `core`：配置、日志、安全、异常。
- `providers`：AI 适配器、外部服务封装。

## 前端分层

```text
src
├─ api
├─ assets
├─ components
├─ layouts
├─ router
├─ stores
├─ views
├─ types
└─ utils
```

职责：

- `api`：封装 HTTP 请求。
- `stores`：Pinia 状态管理。
- `views`：页面级组件。
- `components`：通用组件和业务组件。
- `router`：路由和权限守卫。
- `types`：前端类型定义，与 API 文档保持一致。

## AI Gateway

所有 AI 调用必须经过后端 AI Gateway。

```text
AI Request
  -> 场景校验
  -> 权限校验
  -> 输入脱敏
  -> Provider Adapter
  -> 输出安全检查
  -> 人工审核状态
  -> 调用留痕
```

不允许前端直接调用桂教通或其他 AI API。

## 数据隔离模型

数据范围按组织层级隔离：

```text
region -> school -> class -> project -> task -> submission
```

查询时必须同时满足：

- 用户角色权限。
- 用户组织范围。
- 资源所属范围。
- 项目或班级授权关系。

## 低配服务器策略

- Uvicorn 使用 1 个 worker 起步。
- PostgreSQL 设置较小连接池，例如 `pool_size=5`。
- 上传文件限制默认 20MB。
- 列表接口必须分页。
- 驾驶舱统计优先使用轻量 SQL 聚合，不引入实时大屏流式刷新。
- AI 调用设置超时和频率限制，避免阻塞后端。
- 静态前端由 Nginx 托管，不运行 Node 进程。

## 可扩展方向

后续规模扩大时可逐步引入：

- Redis：缓存、限流、异步队列。
- MinIO/OSS：文件对象存储。
- Celery/RQ：长任务处理。
- Prometheus/Grafana：监控。
- 多实例后端和独立数据库服务器。

第一版不引入这些组件。

